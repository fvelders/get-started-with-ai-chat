# Frontend configuratie voor gescheiden deployment

# Backend API URL
# Laat leeg voor same-origin deployment (backend en frontend op dezelfde server)
# Vul in met het adres van je NAS backend voor gescheiden deployment
# Voorbeelden:
#   http://192.168.1.100:8000  (NAS op lokaal netwerk)
#   http://nas.local:8000       (NAS met hostname)
#   https://api.example.com     (externe backend met HTTPS)
VITE_API_BASE_URL=http://192.168.1.100:8000
